# Form delivery (optional). Get a free key at https://web3forms.com
# then add this in Vercel > Project > Settings > Environment Variables.
# Without it, the contact button opens the visitor's mail client instead.
VITE_WEB3FORMS_KEY=
